//
//  ContentView.swift
//  Luminous
//
//  Created by Yuxuan Shi on 2026-06-27.
//

import SwiftUI

/// The app's UI now lives in `RootView`. Kept as a thin alias so existing
/// references and previews still resolve.
struct ContentView: View {
    var body: some View {
        RootView()
    }
}

#Preview {
    ContentView()
}
